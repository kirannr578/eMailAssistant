# Build/dev-only dependencies. End users running the packaged installer
# never need these. Install with:
#     .\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
-r requirements.txt

# PyInstaller bundles Python + the source tree into a frozen exe.
# The dist/EmailAssistant/ output is then wrapped by Inno Setup.
pyinstaller>=6.6.0
